/mnt/h/AETHIEAOPSYS

ROLE LOCK:
LOCAL LOLITA = active working/build surface
AEUSB = authority / ignition / portable route body
AEXHD / AE320 = heavy-lift / cache / model / mirror body

CLI FIRST:
localhost before dashboard
OpenSSH before SaaS exposure
Cloudflare tunnel only by deliberate route
TOOLS install into ÆOpSys logic, not above it
